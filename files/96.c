#include <stdio.h>

int main()
{
    int num = 0;

    printf("num = %d\n", num);

    num = num + 1; // INC(num)
    printf("num = %d\n", num);

    num++; // INC(num)
    printf("num = %d\n", num);

    ++num; // INC(num)
    printf("num = %d\n", num);

    return 0;
}
